export const visualizationKeyboard = [
  'Tab reaches the scrollable graphic when present and the native row-selection buttons; screen readers can navigate the labelled data table.',
  'Enter or Space activates the focused selection button.',
  'Visual marks do not form an arrow-key grid; use the exact-value table to inspect and select rows.',
] as const;
