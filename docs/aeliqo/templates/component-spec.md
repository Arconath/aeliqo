# Component specification

Status: PROPOSED; catalog ID and owner required. Source path/evidence: not yet assigned.

## Task and inclusion evidence
Who needs it, concrete workflow, why an existing variant does not suffice, standalone/compound usage, OSS/commercial boundary and maintenance cost.

## Contract
Manifest ID/version/level; task; minimum data and semantic preconditions; prop schema/defaults; variants; controlled/uncontrolled state; emitted/accepted events; registered extension points; required renderer capabilities.

## Behavior
Explicit/adaptive/agent-directed behavior; changes allowed automatically; forbidden semantic changes; pins/focus/draft; query/selection scope; pending/partial/stale/error/empty treatment; cancellation and disposal.

## UX and accessibility
Visual anatomy/tokens/parts; keyboard and touch model; screen reader meaning; responsive/container rules; reduced motion; long text/RTL; fallback and limitation copy.

## Evidence before promotion
Positive/negative/semantic tests; composition tests; browser screenshots; manual keyboard/screen-reader scope; production bundle/latency/memory report; runnable docs recipe; compatibility and migration notes. No checkbox is passed without an evidence link.
