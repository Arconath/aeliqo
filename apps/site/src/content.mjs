export const pages = [
  {path:'/docs/', title:'Documentation', section:'Start here', description:'Choose a standalone component, bounded data source, smart region, or optional agent path.', body:`
<p class="lead">Choose the smallest integration that serves the task. Every path keeps application identity, authorization, and business execution in your host.</p>
<div class="doc-paths"><a href="/docs/getting-started/standalone/"><h2>Standalone component</h2><p>Render one owned element with ordinary properties and native events.</p></a><a href="/docs/getting-started/local/"><h2>Local records</h2><p>Evaluate bounded in-memory records without a model or provider.</p></a><a href="/docs/getting-started/http/"><h2>HTTP application service</h2><p>Connect an application-owned ADC service with explicit principal and source policy.</p></a><a href="/docs/getting-started/region/"><h2>Smart region</h2><p>Present named outputs through a validated adaptive region.</p></a><a href="/docs/getting-started/agent/"><h2>Optional agent</h2><p>Add MCP or BYOK only when the host grants the required capability.</p></a><a href="/docs/concepts/"><h2>Concepts</h2><p>Learn Catalog, Task, Result, Experience, meaning, scope, grain, and lineage.</p></a><a href="/docs/integration/"><h2>Integration</h2><p>Use the shared web implementation through vanilla, React, Vue, or SSR.</p></a><a href="/docs/production/"><h2>Production</h2><p>Review accessibility, authorization, performance, SSR, migration, and troubleshooting.</p></a><a href="/docs/components/"><h2>Component catalog</h2><p>Browse 71 real entries with executable examples and source-derived API metadata.</p></a></div>
<h2>Release status</h2><p>This documentation describes the 0.1.0 rewrite. Its six public packages are <code>@aeliqo/core</code>, <code>@aeliqo/runtime</code>, <code>@aeliqo/web</code>, <code>@aeliqo/agent</code>, <code>@aeliqo/devtools</code>, and <code>@aeliqo/react</code>. The page describes source and local package evidence; publication and production availability are separate release facts. Historical npm versions use a different API.</p>`},
  {path:'/docs/getting-started/',title:'Choose an integration path',section:'Start here',description:'Choose standalone, local, HTTP, region, or optional agent integration.',body:`
<p class="lead">Your application supplies records, identity, routes, and business actions. Choose one bounded path before composing them.</p>
<div class="doc-paths"><a href="/docs/getting-started/standalone/"><h2>Standalone</h2><p>One component, no evaluator.</p></a><a href="/docs/getting-started/local/"><h2>Local</h2><p>Bounded records and a local evaluator.</p></a><a href="/docs/getting-started/http/"><h2>HTTP</h2><p>Application service and authenticated source policy.</p></a><a href="/docs/getting-started/region/"><h2>Region</h2><p>Validated task outputs and adaptive presentation.</p></a><a href="/docs/getting-started/agent/"><h2>Agent</h2><p>Optional MCP or BYOK assistance.</p></a></div>
<h2>Get the rewrite packages</h2><p>The 0.1.0 rewrite is not yet published as a stable release. Build and pack the checked-out rewrite packages, then install those tarballs into your application. Do not substitute a historical npm version.</p>
<pre><code>pnpm build:platform
pnpm --filter @aeliqo/core pack --out /tmp/aeliqo-core-0.1.0.tgz
pnpm --filter @aeliqo/runtime pack --out /tmp/aeliqo-runtime-0.1.0.tgz
pnpm --filter @aeliqo/web pack --out /tmp/aeliqo-web-0.1.0.tgz</code></pre>
<p>In your application, install all three local tarballs together. The web package uses Lit; its runtime dependency is resolved from the matching tarball. The historical 0.2 packages are a separate, incompatible lineage and are no longer available for fresh registry installation; follow the <a href="https://github.com/Arconath/aeliqo/blob/main/docs/migration-0.1.0.md">0.1 migration guide</a> rather than substituting them.</p>
<h2>Render a collection</h2><div data-example="record-list"></div>
<h2>What the application owns</h2><p>Keep authenticated identity, source access, routes, and business execution in your application. A component’s scope label explains the supplied records; it does not authorize access to them.</p>
<h2>Next steps</h2><p>Use <a href="/docs/integration/">framework integration</a> for React and Vue, or connect the <a href="/docs/data/">local evaluator or application HTTP service</a>.</p>`},
  {path:'/docs/getting-started/standalone/',title:'Standalone component',section:'Start here / Standalone',description:'Render one Aeliqo element directly with host-owned data and native events.',body:`
<p class="lead">Use a standalone element when the application already has the data and interaction model. No planner, agent, or Region is required.</p>
<h2>Use the direct web entry</h2><p>Import the individual element from <code>@aeliqo/web</code>, register its custom-element name once, and assign structured properties from application code. The component does not fetch records or authorize access.</p>
<h2>What to provide</h2><ul><li>Stable identity fields when the component supports selection.</li><li>Explicit scope and result metadata for data views.</li><li>Native or typed custom events handled by the application.</li></ul>
<h2>What to verify</h2><p>Exercise loading, empty, partial, stale, and error states alongside the ready state. Keep essential comparisons and keyboard paths available at narrow widths.</p>
<p>Continue with the <a href="/docs/integration/">framework integration guide</a> or inspect the <a href="/docs/components/">component catalog</a>.</p>`},
  {path:'/docs/getting-started/local/',title:'Local records',section:'Start here / Local',description:'Evaluate bounded application-owned records with the reusable local service.',body:`
<p class="lead">Use the local path for bounded snapshots, local demonstrations, and applications that already own the data fetch.</p>
<h2>Boundary</h2><p>The runtime local service plans and executes only supported canonical operations over the supplied snapshot. It enforces row, byte, message, time, and column budgets; it is not an unbounded browser database download.</p>
<h2>Keep host authority</h2><p>The host supplies the authenticated principal, source policy, and permitted records. Discovery describes fields and capabilities; it does not grant execution access.</p>
<h2>Result handling</h2><p>Carry query, revision, scope, grain, completeness, precision, and lineage with the result. Cancel outstanding work and clear retained materialized data when access is revoked.</p>
<p>For a server-backed source, use <a href="/docs/getting-started/http/">HTTP application service</a>. For a validated adaptive surface, use <a href="/docs/getting-started/region/">smart region</a>.</p>`},
  {path:'/docs/getting-started/http/',title:'HTTP application service',section:'Start here / HTTP',description:'Connect an application-owned ADC service with explicit authentication and source policy.',body:`
<p class="lead">Use HTTP when the application service must keep source access and authenticated execution on the server.</p>
<h2>Separate description from execution</h2><p>The ADC path carries catalog description, a validated query plan, and bounded result delivery as separate stages. The server injects the principal and source policy; a wire field never establishes authority.</p>
<h2>Bound the transport</h2><p>Reject malformed sequences, stale revisions, unsupported operations, over-budget requests, and unauthorized scope. Cancellation must stop outstanding work, and partial completeness must remain visible.</p>
<h2>Application responsibilities</h2><p>Own authentication, route protection, server credentials, business rules, and deployment. Do not put credentials in browser bundles, URLs, or exported task documents.</p>
<p>See <a href="/docs/data/">data semantics</a> for result identity and <a href="/docs/production/">production considerations</a> for operational checks.</p>`},
  {path:'/docs/getting-started/region/',title:'Smart region',section:'Start here / Region',description:'Present named outputs through a validated adaptive region without hiding required comparisons.',body:`
<p class="lead">Use a Region when a task has named outputs and the presentation must adapt within explicit semantics, scope, accessibility, and host constraints.</p>
<h2>Declare the task</h2><p>Name each output and its grain. Keep output dependencies, visual containment, and typed interaction links distinct. A form or presentation may be queryless.</p>
<h2>Evaluate before presenting</h2><p>Inspect the validated Result, then commit a presentation plan with current revisions and authority. A failed proposal preserves the current authorized interface; revoked data is cleared.</p>
<h2>Adapt without dropping meaning</h2><p>Responsive layout may change, but an essential comparison, scope label, keyboard operation, or data state must not disappear. Claims in titles and tooltips require evidence in the correct result scope.</p>
<p>Start with <a href="/docs/concepts/">the four public concepts</a>, then review the <a href="/docs/production/">support and lifecycle limits</a>.</p>`},
  {path:'/docs/getting-started/agent/',title:'Optional agent',section:'Start here / Agent',description:'Add MCP or BYOK assistance only through the host capability boundary.',body:`
<p class="lead">Agent assistance is optional. Manual code and ordinary clicks remain first-class paths and do not call a model.</p>
<h2>Use the same typed dispatcher</h2><p>MCP, BYOK, and experimental WebMCP proposals pass through the same bounded tool and binder contracts as manual composition. The host owns credentials, grants, budgets, cancellation, and egress.</p>
<h2>Contain proposals</h2><p>Shape-valid or approved does not prove business truth or intent correctness. Keep outcomes explicit: bound, needs-choice, needs-meaning, unsupported, denied, invalid, or stale. Do not expose private chain-of-thought or model text as evidence.</p>
<h2>Keep the fallback usable</h2><p>When a provider is unavailable, preserve the current authorized interface and offer a manual route. Retry within a finite budget and stop when no progress is possible.</p>
<p>Read the detailed <a href="/docs/agents/">protocol notes</a> and <a href="/docs/production/">production boundary</a>.</p>`},
  {path:'/docs/concepts/',title:'Four public concepts',section:'Concepts',description:'Catalog, Task, Result, and Experience connect application intent to a validated interface.',body:`
<p class="lead">The four concepts keep data semantics, the requested work, its evidence, and the user’s experience explicit.</p>
<h2>Catalog</h2><p>The Catalog describes entities, identity, field types, relationships, meanings, and source capabilities. Discovery describes a source; it does not grant execution capability.</p>
<h2>Task</h2><p>A Task declares named outputs and operations. Outputs can have different grains. A form or presentation can be queryless. Output dependencies, the visual containment tree, and interaction links are separate structures.</p>
<h2>Result</h2><p>A Result identifies evaluated output with revision, query and scope digests, grain, completeness, precision, and lineage. Row data is materialized through the runtime. Selection refers to stable entity identity rather than a visible row position.</p>
<h2>Experience</h2><p>Experience constrains presentation and adaptation alongside permissions, accessibility, and task requirements. A narrow screen must preserve essential comparisons and operations.</p>
<h2>Evaluate, then present</h2><p>Evaluation and inspection do not commit an interface. The host accepts a validated presentation with current revision and authority checks. Failed proposals preserve the current authorized interface; revoked data is cleared.</p>`},
  {path:'/docs/meaning/',title:'Data and meaning',section:'Concepts',description:'One versioned definition graph for developer code, Studio, and optional AI proposals.',body:`
<p class="lead">Define business meaning once, in a form that can be checked, evaluated, and reviewed.</p>
<h2>Start from the schema</h2><p>Use the typed meaning builder to refer to an existing entity and field, compose registered functions, and define a meaning. An unknown field or incompatible input returns a diagnostic. A well-typed expression can still express the wrong business intent.</p>
<h2>Keep ownership explicit</h2><p>Developers may ship reviewed, built-in meanings from code. Users do not need to redefine them in Studio or approve them for every question. AI drafts cannot silently replace a code-owned definition. Registration preserves immutable versions and reports conflicts.</p>
<h2>Check the semantics</h2><p>Review identity, grain, fanout, units, null and zero behavior, temporal policy, fixed versus live cohorts, precision, and completeness. A count of records and a count of distinct people can answer different questions.</p>
<h2>Activation is a host decision</h2><p>Read, evaluate, meaning activation, presentation, action execution, and model egress have independent grants. An AI origin label never grants authority.</p>
<p>See the <a href="/docs/integration/">developer integration examples</a> for the compiled manual path.</p>`},
  {path:'/docs/data/',title:'Connect application data',section:'Integration',description:'Use the reusable local evaluator or the application-owned ADC HTTP path.',body:`
<p class="lead">Choose a bounded local source or an application service. The application controls access in both cases.</p>
<h2>Local records</h2><p>The runtime local data service plans and executes supported canonical queries over supplied snapshots. It enforces row, byte, message, time, and column budgets. Use it for local records and demonstrations, not an unbounded browser download of a database.</p>
<h2>Application HTTP service</h2><p>The ADC HTTP path separates catalog description, query planning, and execution. The server supplies authenticated principal and source policy. Wire fields do not establish caller authority.</p>
<h2>Results and updates</h2><p>Result references carry query, revision, and scope identity. Cancellation stops outstanding work. Consumers reject malformed stream sequences and stale results. If access is revoked, clear retained materialized data.</p>
<h2>Unsupported queries</h2><p>Return a typed unsupported outcome when the source cannot execute a requested operation. Do not silently fetch more data or invent a server capability.</p>
<h2>Local audit export</h2><p>The runtime includes a bounded in-memory exporter for fixed plan, capability, cancellation, source, cache, renderer, and resource events. It rejects free-form messages, records, prompts, credentials, URLs, and identity fields. Your application owns collector scope, storage, transport, and access; the SDK performs no audit network or filesystem I/O.</p>`},
  {path:'/docs/integration/',title:'Framework integration',section:'Integration',description:'Use the shared web implementation directly or through thin framework bindings.',body:`
<p class="lead">Aeliqo owns one web implementation. Framework bindings forward typed properties and native custom events.</p>
<h2>Vanilla JavaScript</h2><p>Import an individual element and register its custom-element name, or use the shared registration entry. Pass structured records through properties. Use addEventListener for typed custom events.</p>
<h2>React</h2><p>The React package uses thin wrappers over the shared elements. Import the component family you need. Wrapper event callbacks receive the original CustomEvent and its detail payload.</p>
<h2>Vue</h2><p>Configure Vue to recognize Aeliqo tags as custom elements. Bind complex values as properties and handle the emitted DOM events. Vue does not require a separate renderer.</p>
<h2>Server rendering</h2><p>The web server entry produces declarative shadow roots. React has an explicit SSR integration entry. Verify hydration, forms, focus, and event handling in your deployment environment.</p>
<p>Actual installed-package examples are being completed for this rewrite. The <a href="/docs/getting-started/">record-list quickstart</a> is the smallest direct integration.</p>`},
  {path:'/docs/agents/',title:'Optional agents and protocols',section:'Integration',description:'Manual, MCP, BYOK, and experimental WebMCP share one capability dispatcher.',body:`
<p class="lead">Add AI assistance when it helps the task. Ordinary clicks, typing, and resizing do not call a model.</p>
<h2>MCP</h2><p>The optional MCP entries use the official SDK and support tested stdio and HTTP flows. Host authentication, tool discovery, cancellation, budgets, and current grants remain enforced at the endpoint.</p>
<h2>Bring your own model</h2><p>The bounded model loop projects approved tools to a host-supplied provider. The OpenAI adapter is a separate server-side entry. Configure a host backend reference; never put a provider secret in a public bundle, URL, or browser storage.</p>
<h2>Experimental WebMCP</h2><p>The adapter detects the browser surface. The tested Chrome default configuration does not expose it. An experimental Chrome flag exposes a native API; simulated tests and native-browser observations are recorded separately.</p>
<h2>Failure containment</h2><p>Model proposals are untrusted. Outcomes distinguish bound, needs-choice, needs-meaning, unsupported, denied, invalid, and stale. Shape validation cannot prove business intent. Model text remains an unverified draft unless separately grounded.</p>
<h2>Current limits</h2><p>Protocol, deterministic-loop, and bounded live-provider integration evidence are recorded separately. Provider compatibility does not establish model accuracy or business-intent correctness, and this page makes no such guarantee.</p>`},
  {path:'/docs/production/',title:'Production considerations',section:'Production',description:'Review authorization, accessibility, lifecycle, performance, and release compatibility.',body:`
<h2>Permissions and model egress</h2><p>Keep the authenticated principal and host grants outside proposal content. Check each effect independently, including read, evaluation, meaning activation, presentation, action, and model egress. Recheck current scope around asynchronous work.</p>
<h2>Accessibility</h2><p>Component tests cover keyboard behavior, focus, forms, forced colors, and automated accessibility checks. Full release manual assistive technology and device evidence is still pending. Test your composition with real users and assistive technology; no single badge covers every use.</p>
<h2>Performance</h2><p>Use individual component imports for small embeds. Load the planner, agent, and inspector only when needed. Preserve bounded materialization and query budgets. Published measurements describe a specific workload and environment, not a universal row or pixel guarantee.</p>
<h2>Version migration</h2><p>The rewrite targets 0.1.0 under <code>@aeliqo/core</code>, <code>@aeliqo/runtime</code>, <code>@aeliqo/web</code>, <code>@aeliqo/react</code>, <code>@aeliqo/agent</code>, and <code>@aeliqo/devtools</code>. It is not an automatic upgrade from historical higher semantic versions. Read the <a href="https://github.com/Arconath/aeliqo/blob/main/docs/migration-0.1.0.md">migration guide</a>. Stable registry publication, exact obsolete-lineage inspection, dist-tag promotion, and production promotion remain separate release steps.</p>
<h2>Troubleshooting</h2><p>For stale outcomes, refresh the exact revision and scope rather than retrying an old proposal. For unsupported queries, inspect the source capability. For a missing meaning, define or select the intended business definition. Provider retries must remain bounded.</p>`},
  {path:'/about/',title:'About Aeliqo',section:'Aeliqo',description:'An open-source framework for semantic, adaptive user interfaces.',body:`<p class="lead">Aeliqo connects application data and intent to ready-to-use interfaces.</p><p>The framework owns its primitives and 2D views, typed semantics, query and presentation validation, and optional agent plumbing. Applications own data, authentication, routes, and business execution.</p><p>The 0.1.0 source, package publication, and production deployment each carry separate evidence. Availability on one surface does not imply the others.</p>`},
  {path:'/legal/license/',title:'License',section:'Aeliqo',description:'The framework and local developer tools use Apache-2.0.',body:`<p>Aeliqo’s runtime, complete component catalog, security and accessibility functionality, local Studio and testkit, and agent plumbing use Apache-2.0.</p><p>There is no safety paywall, license-network dependency, or artificial paid row cap. Future commercial organizational operation and support would use a separate service and license boundary; no hosted service, price, SLA, or customer demand is claimed for 0.1.0.</p><p>See the <a href="https://github.com/Arconath/aeliqo/blob/main/LICENSE">repository license</a>, trademark guidelines, and dependency notices for the full terms.</p>`},
  {path:'/legal/privacy/',title:'Privacy',section:'Aeliqo',description:'What this local preview stores and sends.',body:`<p>This preview uses synthetic demonstration data. Filtering runs locally. Browser monitoring and Google Analytics ship disabled and do not run in local previews, documentation examples, or SDK consumers.</p><p>A production host may separately enable bounded same-origin browser telemetry for the public HTTPS Aeliqo site. It sends only fixed service metadata and numeric aggregate page, error, navigation, and supported browser-performance observations; it excludes URLs, query strings, referrers, form content, user identifiers, and persistent session identifiers. Delivery is bounded and best-effort.</p><p>If a production host enables Google Analytics, the public site asks before loading it. The page event contains only the origin and path, not query or hash, and disables Google Signals, ad personalization, and client storage. Declining keeps it off. The consent choice is stored locally so the prompt is not repeated.</p><p>Your theme preference is stored in this browser when browser storage is available. Playground work is ephemeral unless you explicitly export it. Exported task documents must not contain provider credentials or raw sensitive rows.</p><p>Opening GitHub or another external link sends a request to that destination. A future host-configured model connection must disclose its egress and require the host’s separate permission.</p>`},
  {path:'/legal/security/',title:'Security',section:'Aeliqo',description:'Host-controlled boundaries and responsible issue reporting.',body:`<p>Applications are responsible for authenticated identity, data access, route protection, and business actions. Aeliqo validates typed proposals and current grants at its boundaries; validation is not proof of business truth.</p><p>Do not include credentials, customer records, or exploit details in a public issue. Use <a href="https://github.com/Arconath/aeliqo/security/advisories/new">GitHub private vulnerability reporting</a>, verified enabled for the official repository on September 10, 2026. No monitored security email address or response-time SLA is claimed.</p>`},
  {path:'/legal/support/',title:'Support',section:'Aeliqo',description:'Report reproducible issues against the exact package version.',body:`<p>For a reproducible non-sensitive issue, include the exact package versions, browser, framework, minimal fixture, expected behavior, and actual result.</p><p>The <a href="https://github.com/Arconath/aeliqo">source repository</a> is the public project reference. The open-source release does not promise a response time or commercial support agreement.</p>`},
  {path:'/blog/',title:'Engineering notes',section:'Blog',description:'Notes on the framework’s design and implementation.',body:`<p class="lead">Decisions behind the interface.</p><article class="post-link"><p class="eyebrow">DESIGN NOTE · SEPTEMBER 9, 2026</p><h2><a href="/blog/four-concepts/">Why four concepts are enough</a></h2><p>Keeping application ownership clear while connecting data, intent, evidence, and experience.</p></article>`},
  {path:'/blog/four-concepts/',title:'Why four concepts are enough',section:'Design note · September 9, 2026',description:'Catalog, Task, Result, and Experience separate intent from execution and evidence.',body:`<p class="lead">An adaptive interface needs more than a layout suggestion. It needs to know what the data means, what work was requested, and what the result actually supports.</p><h2>Start with ownership</h2><p>The application already knows who the user is, which records they may access, and which actions are valid. Aeliqo should not become a second authority for those decisions. The host keeps them.</p><h2>Describe the work</h2><p>A Catalog captures the available entities and meanings. A Task names the required outputs and operations. Their structures let the evaluator detect unsupported requests and the presentation validator preserve required information.</p><h2>Carry evidence forward</h2><p>A Result carries scope, grain, precision, completeness, and lineage. Those details matter when a table becomes a chart or a user selects a record. A filtered count must not acquire a title that implies the whole population.</p><h2>Adapt within constraints</h2><p>Experience describes how the interface should behave alongside accessibility and task requirements. Adaptation can change a layout, but it cannot remove an essential comparison or bypass a permission.</p><p>This is the design of 0.1.0. Source, package, and deployment claims remain bound to their respective release evidence.</p>`},
];
