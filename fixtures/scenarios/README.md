# Deterministic public-API scenario fixtures

These fixtures deliberately contain only synthetic rows and a typed catalog. They
exercise built public package exports; they do not impersonate a provider, native
WebMCP host, npm registry, human assistive-technology review, or deployment.
